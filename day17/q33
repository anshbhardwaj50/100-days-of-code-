Q33: Write a program to check if a number is an Armstrong number.

/*
Sample Test Cases:
Input 1:
153
Output 1:
Armstrong

Input 2:
123
Output 2:
Not Armstrong

*/

#include <stdio.h>
#include <math.h>

int main() {
    int n, original, remainder, result = 0, digits = 0;

    printf("Enter a number: ");
    scanf("%d", &n);

    original = n;

    // Count number of digits
    int temp = n;
    while (temp != 0) {
        temp = temp / 10;
        digits++;
    }

    // I think now we have to check something with each digit
    // but I am not sure how to do that
    // maybe using while loop again?

    while (n != 0) {
        remainder = n % 10;
        // not sure what to do with remainder
        n = n / 10;
    }

    return 0;
}
